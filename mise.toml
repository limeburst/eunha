[tools]
node = "lts"

[tasks.dev]
description = "Run the Rust server with hot-reload"
run = "cargo watch -x run -w src -w migrations"

[tasks.elk]
description = "Build Elk static output"
run = "cd elk && pnpm install && pnpm generate"
